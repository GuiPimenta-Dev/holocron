# data/ = cached wikitext + LanceDB + Neo4j volume — rebuilt via ingest, never committed
data/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.env

# eval/runs/ = per-run answer artifacts — only baselines and the golden set are versioned
eval/runs/
